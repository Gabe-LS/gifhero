[package]
name = "imagequant-gif-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
imagequant = { version = "4", default-features = false }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
